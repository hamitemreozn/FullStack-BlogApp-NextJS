# Legacy source archive

This folder preserves the retired application structure as a code-review reference. It is not imported, built, or deployed.

Customer-linked Firebase and OAuth configuration was deliberately removed from the archived utility files. The active application is in `src/` and uses only the local PostgreSQL, Better Auth, and MinIO configuration described in the project root documentation.
